@model List<ScoreBoard.ViewModels.DashboardViewModel>

@{
    ViewData["Title"] = "Dashboard";
}

@foreach (var item in Model)
{
    <h1>Welcome to the Dashboard!</h1>
    <p>This is the dashboard page content.</p>
}
 is the dashboard page content.</p>